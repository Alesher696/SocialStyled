import React from 'react';
import styled from "styled-components";

export const Users = () => {
    return (
        <UsersWrapper>
            users
        </UsersWrapper>
    );
};

const UsersWrapper = styled.div`
  display: flex;
  color: white;
  font-size: 24px;
  margin: 0 auto;
`